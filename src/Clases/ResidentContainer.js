import React from 'react';

function ResidentContainer(/* {url} */) {

    return(
        <>
            {/* { url.forEach(element => {
                <Item url={elemet} />
            })} */}
        </>
    )

}

export default ResidentContainer;